# Evaluation set status

The evaluation code and protocol are provided under `benchmarks/`. Published metric values must come from versioned run JSON and human labels; no placeholder value is represented as a result.

