# WP3–WP4 specification

Merge chunks from up to six videos while preserving video/title/start/end metadata. Continue after per-video failures. Render every supported claim with YouTube `t=Ns` links and expandable source text; suppress unsupported claims.

