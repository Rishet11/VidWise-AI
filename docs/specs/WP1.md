# WP1 specification

Cache by video ID before network use. Live uncached fetches use Supadata timestamped native captions. Upload supports SRT, VTT, JSON, and TXT. Local YouTube fetching is opt-in only. Measure T1 and T3 separately on at least 20 varied public videos from the deployment environment.

